'use strict';

describe('basket service', function () {
  //var BasketService = require('./basket.js');

  it('should retrieve the basket product list', function () {
    expect(2 + 2).toEqual(4);
  });
});
